- name: Checkout
  uses: actions/checkout@v2
  with:
    fetch-depth: 0 # <-- clone with complete history

- name: Push
  uses: heussd/mirror-to-bitbucket-github-action@v2
  with:
    username: ritaban2006
    spacename: College
    password: ${{ secrets.BITBUCKET_APP_PASSWORD }}
